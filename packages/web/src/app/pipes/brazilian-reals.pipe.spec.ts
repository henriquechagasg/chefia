import { BrazilianRealsPipe } from './brazilian-reals.pipe';

describe('BrazilianRealsPipe', () => {
  it('create an instance', () => {
    const pipe = new BrazilianRealsPipe();
    expect(pipe).toBeTruthy();
  });
});
